import { TotalItemsPipe } from './total-items.pipe';

describe('TotalItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalItemsPipe();
    expect(pipe).toBeTruthy();
  });
});

import { PizzaPropPipe } from './pizza-prop.pipe';

describe('PizzaPropPipe', () => {
  it('create an instance', () => {
    const pipe = new PizzaPropPipe();
    expect(pipe).toBeTruthy();
  });
});

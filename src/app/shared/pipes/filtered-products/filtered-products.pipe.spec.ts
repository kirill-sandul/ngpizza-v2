import { FilteredProductsPipe } from './filtered-products.pipe';

describe('FilteredProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
